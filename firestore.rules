]rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /Submissions_{userId}/{document=**} {
      allow read, write: if request.auth != null && userId == encodeURIComponent(request.auth.token.email);
    }
  }
}
